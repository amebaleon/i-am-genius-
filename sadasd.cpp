#include <stdio.h>
int isum(int a, int b);
void hprint(void);
int main(void)
{
	int ia, ib, sum;
	hprint();
	while (scanf("%d%d", &ia, &ib)== 2){
		sum = isum(ia, ib);
		printf("%d과 %d의 합은 = %d\n", ia, ib, sum);
	    hprint();
	    if(n!=2)
	return 0;

}
